---
title: Discord画像圧縮くん — ポートフォリオ
created: 2026-08-12
updated: 2026-08-15
status: active
---

# 🖼️ Discord画像圧縮くん — 開発ポートフォリオ

> Discord無料プランの添付ファイル20MB制限に向けた、ブラウザ完結型の画像・動画圧縮Webアプリ。画像はRust + WebAssembly、動画はWebCodecs APIによるハードウェアエンコードで、**679MBの4K HEVC動画を6.94MBに75秒で圧縮**する。

**公開URL:** https://moruton1119.github.io/discord-image-compressor/  
**リポジトリ:** https://github.com/moruton1119/discord-image-compressor  
**ライセンス:** MIT

---

## 📊 実測パフォーマンス（本番テスト結果）

| テスト動画 | 元サイズ | 解像度 | コーデック | 結果 | 圧縮率 | 処理時間 |
|-----------|---------|--------|-----------|------|--------|---------|
| 1362.mp4 | **679.77MB** | 3840x2160 (4K) | HEVC/H.265 | **6.94MB** | **98.9%減** | 75秒 |
| 忍者ギミックPV0814.mp4 | 393.12MB | 1920x1080 | H.264 | 9.61MB | 97.6%減 | 40秒 |
| 1435.mp4 | 395.05MB | 3840x2160 (4K) | HEVC/H.265 | 6.94MB | 98.2%減 | 120秒→75秒 |
| 1322.mp4 | 140.84MB | 2160x3840 (縦4K) | HEVC/H.265 | 4.43MB | 96.9%減 | 13秒 |

※すべてスマホ実機（Android / Linux Chrome）で測定。サーバー通信ゼロ・完全ローカル処理。

---

## 💡 開発の背景

Discordの無料プランでは添付ファイルが**20MBまで**。画像を投稿しようとすると制限に引っかかることが多く、毎回画像編集ソフトで手動圧縮するのが面倒だった。

既存の圧縮ツールは「サーバーにアップロードして処理する」ものが多く、プライバシー面でも不安があった。そこで**完全ローカル処理（サーバー通信ゼロ）**で、かつ**爆速**な圧縮ツールを開発することにした。

---

## 🎯 要件定義

| 要件 | 詳細 |
|------|------|
| 目標 | 画像・動画を20MB以下に自動圧縮 |
| プライバシー | **完全ローカル処理**（サーバーへのファイル送信なし） |
| 品質 | 20MBに可能な限りギリギリ寄せる（画質を保持） |
| UX | ドラッグ&ドロップだけで完結 |
| コスト | **運営費¥0**（静的ホスティングのみ） |

---

## 🏗️ 技術アーキテクチャ

### 全体構成

```
┌──────────────────────────────────────────────────────┐
│                  ブラウザ（ユーザー側）                 │
│                                                      │
│  ┌───────────┐                                       │
│  │  UI (HTML) │  ── タブ切り替え ──┐                   │
│  │  ドラッグ&  │                    │                   │
│  │  ドロップ   │                    │                   │
│  └───────────┘                     │                   │
│       │                            │                   │
│       ▼                            ▼                   │
│  ┌──────────────────┐   ┌────────────────────────┐   │
│  │ 🖼️ Rust/WASM     │   │ 🎬 WebCodecs パイプライン │   │
│  │   (画像圧縮)      │   │                        │   │
│  │  ・JPEG二分探索   │   │ mp4box.js (デマックス)    │   │
│  │  ・Lanczos3リサイズ│   │   ↓                    │   │
│  │  ・WebP可逆圧縮   │   │ VideoDecoder (HW/HEVC) │   │
│  └──────────────────┘   │   ↓ Canvas (リサイズ)     │   │
│                         │   ↓                    │   │
│                         │ VideoEncoder (HW/H.264) │   │
│                         │   ↓                    │   │
│                         │ mp4-muxer (MP4格納)      │   │
│                         └────────────────────────┘   │
│       │                            │                   │
│       └──────── ファイル送信なし ────┘                   │
└──────────────────────────────────────────────────────┘
                          │
                   ┌──────▼──────┐
                   │ GitHub Pages │  (静的ファイル配信のみ)
                   │   無料・HTTPS │  処理は一切行わない
                   └─────────────┘
```

### 「完全ローカル動作」の仕組み

このアプリは**サーバー側で一切の処理を行わない**。GitHub PagesはHTML・JS・WASMファイルを配信する「倉庫」にすぎない。

| 読み込み | 処理 | 送信 |
|---------|------|------|
| ブラウザがHTML/JS/WASMをダウンロード | **全てブラウザ内（WASM）で実行** | **画像・動画データは1バイトもサーバーに送信されない** |

**重要:** JavaScriptで処理をしているのではなく、**Rust（画像）とC（動画）をWebAssemblyにコンパイルしてブラウザ内でネイティブ実行**している。JSはUI操作とWASMへのデータ受け渡し役のみ。

### 技術スタック

| レイヤー | 技術 | 選定理由 |
|---------|------|---------|
| 画像圧縮エンジン | **Rust + WebAssembly** | ネイティブ速度・安全・ポータブル |
| 画像処理ライブラリ | **image crate (Rust)** | JPEG/PNG/WebP/BMP/GIF対応・実績豊富 |
| 動画圧縮エンジン | **WebCodecs API** | ブラウザのハードウェアコーデックを直接制御。ffmpeg.wasm比で約10倍高速 |
| MP4デマックス | **mp4box.js 2.4.1**（同梱） | MP4コンテナ解析・チャンク抽出 |
| MP4マックス | **mp4-muxer 5.2.2**（同梱） | エンコード済みチャンクからMP4生成 |
| フォールバック | **Canvas + MediaRecorder** | WebCodecs非対応環境向け（自動切替） |
| WASMバインディング | **wasm-pack** (画像) | Rust→WASMのデファクトツールチェーン |
| UI | **Vanilla HTML/CSS/JS** | 依存ライブラリゼロ・軽量・タブ切り替え |
| ホスティング | **GitHub Pages** | 無料・HTTPS自動・CI/CD内蔵・サーバー処理不要 |
| CI/CD | **GitHub Actions** | プッシュ → 約20秒で自動デプロイ |

---

## 🔧 圧縮ロジック（工夫の核心）

### 画像: 二分探索 × 段階的ダウンスケール

このプロジェクトの最大の工夫は**「20MBにギリギリ寄せる」圧縮アルゴリズム**にある。

```
入力画像
    │
    ▼
┌─────────────────────┐
│ Step 1: EXIF削除     │  Rust image crate でデコード
│ (メタデータ全削除)    │  → 再エンコード時に自動で削除
└─────────┬───────────┘
          ▼
┌─────────────────────┐
│ Step 2: 二分探索     │  品質パラメータを binary search
│ JPEG品質 1↔100      │  最大20回の反復で最適品質を発見
│                     │  目標以下で最大品質のものを選ぶ
└─────────┬───────────┘
          ▼
     ┌──── 目標達成？────┐
     │Yes              │No
     ▼                 ▼
   完了!     ┌───────────────────┐
             │ Step 3: 解像度ダウン │  90% → 80% → ... → 10%
             │ Lanczos3フィルタ    │  高品質リサイズで再圧縮
             └─────────┬─────────┘
                       ▼
                 各スケールで二分探索
                       │
                       ▼
                  完了!
```

**なぜ二分探索なのか？**
- 品質パラメータとファイルサイズは単調（品質↑ → サイズ↑）
- 線形探索（1ずつ試す）は最大100回だが、二分探索は**最大7回**で収束
- ユーザー待ち時間を最小化

**なぜLanczos3フィルタなのか？**
- リサイズフィルタの中で最高クラスの画質
- エッジ保存性が高く、ダウンスケール時の劣化が最小
- Rustのimage crateが標準サポート

### 動画: WebCodecs ハードウェアパイプライン × ビットレート逆算

動画圧縮では、画像のような「品質パラメータの二分探索」が使えない（動画はエンコードに時間がかかりすぎる）。そのため**ビットレート逆算**で1回のエンコードで目標サイズに収め、**WebCodecs API**でブラウザのハードウェアコーデックを直接叩いて高速化している。

```
入力動画 (MP4 / HEVC / H.264)
    │
    ▼
┌──────────────────────────┐
│ Step 1: メタデータ取得       │  HTML5 video要素でduration/fps取得
│  20MB以下なら圧縮スキップ    │  （品質保持のため）
└──────────┬───────────────┘
           ▼
┌──────────────────────────┐
│ Step 2: ビットレート逆算     │  映像 = (目標 − 音声実サイズ)×8 / 長さ
│  + 解像度ダウンスケール       │  長い動画は解像度上限に自動制限（1280/1920/元解像度）
│  + 音声抽出（AAC）          │  音声は再エンコードなしでそのままコピー
└──────────┬───────────────┘
           ▼
┌──────────────────────────┐
│ Step 3: mp4box.js デマックス │  MP4コンテナを解析しフレーム単位に分解
│  description取得           │  avcC/hvcCボックスから抽出（W3C公式方式）
└──────────┬───────────────┘
           ▼
┌──────────────────────────┐
│ Step 4: VideoDecoder       │  ハードウェアデコード（HEVC/H.264対応）
│  → Canvas リサイズ         │  ターゲット解像度に縮小
│  → VideoEncoder           │  ハードウェアH.264エンコード（CBR）
└──────────┬───────────────┘
           ▼
┌──────────────────────────┐
│ Step 5: mp4-muxer          │  エンコード済みチャンクをMP4に格納
└──────────┬───────────────┘
           ▼
     ┌── 20MB以下？──┐
     │Yes          │No
     ▼             ▼
   完了!     ┌──────────────────┐
             │ Step 6: 再圧縮    │  実測オーバー率から逆算：
             │ （最大3回）       │  新bitrate = bitrate × 0.95 × 目標/実測
             │ 3回未達は妥協DL   │  （2回目でほぼ収束）
             └────────┬─────────┘
                      ▼
                  完了!
```

**なぜWebCodecsなのか？**
- MediaRecorderより**8〜15倍高速**（ハードウェアエンコード直叩き）
- ffmpeg.wasm比で**約10倍高速**（ソフトウェアWASMエンコード不要）
- HEVC（H.265）入力に対応 — スマホ撮影動画の大部分がHEVC
- SharedArrayBuffer不要（GitHub PagesのCOOP/COEP制約を回避）

**なぜVBR（可変ビットレート）に移行したのか？**
- 当初はCBRを採用。動画のサイズ = ビットレート × 長さで数学的に確定するため、1回のエンコードで確実に目標に収まるから
- しかしCBRはシーン間のビット配分が硬直し、複雑なシーンで画質が落ちる弱点があった
- 実は以前VBRで事故っている（複雑シーンでビットレート2倍超過、834kbps指定→17.41MB出力。目標10MB時代の記録）
- 転機は「実測逆算リトライ」の導入。VBRのサイズ変動を実測から逆算して吸収できるようになったため、画質向上の恩恵だけ得られる構図が成立
- VBR + 実測逆算リトライ（最大3回）の組み合わせで「高画質」と「目標サイズ遵守」を両立（外部コントリビューターPR #1で移行）
- `hardwareAcceleration: 'prefer-hardware'`（GPU優先）と `latencyMode: 'quality'` も同時に導入

**フォールバック設計：**
- WebCodecs非対応ブラウザ → Canvas + MediaRecorder APIに自動切替
- MediaRecorderは2〜4倍速再生で録画時間を短縮（音声もWebAudio経由で録画に含む。スピーカーからは出さない）
- どちらのエンジンも処理フェーズ全体を0〜100%にマッピングしたプログレスバー表示

---

## 🦀 Rust → WebAssembly 採用の経緯

### なぜRustを使ったか

当初はCanvas API（JavaScript）で実装していたが、以下の課題があった：

| 課題 | Canvas API | Rust/WASM |
|------|-----------|-----------|
| 処理速度 | JITの限界 | **ネイティブに近い速度** |
| エンコード制御 | ブラウザ依存（品質指定が雑） | **品質1-100を正確に指定可能** |
| メタデータ処理 | Canvas再描画で削除（間接的） | **デコード時点で確実に削除** |
| 今後の拡張性 | JS固有 | **動画圧縮もRustで統一可能** |

### 参考にしたプロジェクト

**GoogleChromeLabs/squoosh**（Star 20k+）のアーキテクチャを参考にした。Squooshは画像コーデックをRust/C++で書き、wasm-packでWASMにコンパイルしてブラウザで動かしている。そのアプローチを踏襲した。

### WASM ビルド成果物

```
rust-engine/
├── Cargo.toml          # Rustパッケージ定義
├── src/
│   └── lib.rs          # 圧縮エンジン本体（265行）
└── pkg/                # wasm-pack出力
    ├── image_compressor.js        # JS バインディング (11KB)
    ├── image_compressor_bg.wasm   # WASMバイナリ (1.1MB)
    └── image_compressor.d.ts      # TypeScript型定義
```

WASMバイナリは**1.1MB**。圧縮エンジン全体としては十分軽量で、初回ロード後はブラウザにキャッシュされる。

---

## 🎨 UI/UX の工夫

### Discord風ダークテーマ

Discordのブランドカラー（#5865F2）をアクセントに採用し、ユーザーが親しみやすいデザインにした。

### 処理中の体験設計

画像圧縮は数秒かかる場合があるため、「待たされている感」を軽減する工夫をした：

| 施策 | 効果 |
|------|------|
| **全画面オーバーレイ**（スピナー付き） | 処理中であることを明示 |
| **ファイル名・進捗表示**（〇/〇枚目） | あとどれくらいか予測可能 |
| **連続ドロップ防止**（isProcessingフラグ） | 二重処理によるバグを防止 |
| **ドロップエリア無効化**（opacity低下） | 視覚的にも「受け付け中ではない」を表現 |

### 設計上の配慮

| 配慮 | 実装 |
|------|------|
| 画面外ドロップ無効化 | `window.addEventListener('drop', preventDefault)` |
| フォーマット選択 | WebP（推奨）/ JPEG（互換性）※PNGは可逆圧縮でサイズ制限を守れないため非対応 |
| 安全なHTML生成 | `escapeHtml()` でXSS対策 |

---

## 🚀 デプロイ & CI/CD

### GitHub Pages + GitHub Actions

```
git push origin main
       │
       ▼
GitHub Actions 起動
       │
       ├── Checkout
       ├── Configure Pages
       ├── Upload Artifacts (全ファイル)
       └── Deploy to GitHub Pages
                │
                ▼
    https://moruton1119.github.io/discord-image-compressor/
```

プッシュから**約20秒**で本番反映される。サーバー代は**¥0**。

---

## 📊 開発ステップ（タイムライン）

| ステップ | 内容 | 成果物 |
|---------|------|--------|
| **1. 要件定義** | Discord 20MB制限の調査・圧縮ロジック設計 | `ideas/discord-image-compressor.md` |
| **2. Canvas API版（Phase 1）** | HTML/CSS/JS でMVP構築 | `index.html` + `compressor.js` |
| **3. 処理中UX改善** | オーバーレイ・連続ドロップ防止 | UI改善 |
| **4. 技術調査** | Rust+WASM リポジトリ調査（Squoosh等5件） | 調査レポート |
| **5. Rust圧縮エンジン（Phase A）** | wasm-pack でビルド・統合 | `rust-engine/` + WASM 1.1MB |
| **6. CI/CD設定** | GitHub Actions で自動デプロイ | `.github/workflows/deploy.yml` |
| **7. 動画圧縮（Phase B）** | ffmpeg.wasm → 試行錯誤 → MediaRecorder → WebCodecsへ進化 | `video-compressor.js` + タブUI |
| **8. WebCodecs エンジン** | mp4box.js + WebCodecs + mp4-muxerパイプライン構築 | HEVC対応・ハードウェアエンコード |
| **9. HEVC description修正** | W3C公式サンプル準拠の実装でH.265デコード解決 | `file.getTrackById()` + `box.write()` |
| **10. UX仕上げ** | CBR化・全フェーズプログレスバー・パーセント表示 | 1回圧縮で確実に目標達成 |

---

## 🔢 プロジェクト数字

| 指標 | 数値 |
|------|------|
| 総コード行数 | **3,000行以上** |
| Rust エンジン | **265行** |
| UI (HTML/CSS) | **540行** |
| JS統合層 | **app.js 403行** + **compressor-wasm.js 249行** |
| 動画圧縮モジュール | **video-compressor.js 540行**（WebCodecs + MediaRecorder両エンジン） |
| WASMバイナリ（画像） | **1.1MB** (Rust → wasm-pack) |
| 動画エンジンの追加DL | **ゼロ**（WebCodecsはブラウザ内蔵、mp4box/mp4-muxerは同梱） |
| 依存ライブラリ（CDN） | **ゼロ**（mp4box.js・mp4-muxerもvendor/に同梱） |
| サーバー代 | **¥0**（GitHub Pages無料枠） |
| デプロイ時間 | **約20秒**（push → 本番反映） |
| 最大実績 | **679.77MB → 6.94MB（98.9%圧縮）を75秒で** |
| 対応フォーマット | 入力: JPEG/PNG/WebP/GIF/BMP → 出力: WebP/JPEG ・ 動画: MP4/MOV/WebM（H.264/HEVC） |

---

## 🧠 学び・知見

### 1. image crate 0.25 の制約
`image` crate のWebPエンコーダは**losslessのみ**対応。lossy品質指定が必要な場合はJPEGエンコーダ（`new_with_quality`）を使うのが現実的。この制約はSquooshでも `libwebp` を別途使うことで回避している。

### 2. wasm_bindgen の Copy 制約
`#[wasm_bindgen]` 構造体は `Copy` トレイトを要求するため、`String`フィールドを直接持てない。ゲッターメソッド（`#[wasm_bindgen(getter)]`）経由で返すことで回避した。

### 3. 動画圧縮エンジンの進化: ffmpeg.wasm → MediaRecorder → WebCodecs

**ffmpeg.wasm（失敗）**: 約30MBのcoreダウンロード、SharedArrayBuffer必須（COOP/COEPヘッダーがGitHub Pagesで設定不可）、395MB動画でメモリ不足クラッシュ（WASM仮想FSに全ファイル載せが必要）。**7つの技術的問題を突破した末に、メモリ制約が根本的に解決不可と判明し廃止。**

**MediaRecorder（暫定）**: ブラウザネイティブで動くが、実時間ベースの録画のため遅い（2〜4倍速が限界）。

**WebCodecs（最終解）**: ハードウェアコーデックを直接制御。追加ダウンロードゼロ・メモリ安全・最高速。**公式ドキュメントが少なく、W3C公式サンプル（samples/video-decode-display/demuxer_mp4.js）が実装のバイブルになった。**

### 4. HEVC/H.265のdescription取得（最大の難所）

WebCodecsでHEVCをデコードするには `VideoDecoderConfig.description` にhvcCボックスの正しいバイト列が必須。**3つの罠**があった：

1. mp4box.jsの `onReady` が返すtrack infoは軽量版で `mdia` が存在しない → **`file.getTrackById(track.id)`** で内部trackを取得する
2. `box.data` をそのまま渡しても不正 → **`box.write(stream)` でシリアライズ**する
3. シリアライズ結果にはボックスヘッダーが含まれる → **先頭8バイトを削除**する

この3点セットはW3C公式サンプルそのまま。エラーメッセージ（"you must fill out the description field"）からは読み取れない構造的な問題だった。

### 5. VBR vs CBR — サイズ制御の本質

`bitrateMode: 'variable'`（VBR）はビットレートを「目安」として扱う。複雑なシーンで2倍超過し、834kbps指定なのに17.41MB出力（※目標10MB時代の実測）。**サイズ上限が硬直的な制約（当時のDiscordの10MB、現行20MB）の場合、CBR一択。** 品質重視ならVBRだが、再圧縮（2倍の時間）が発生するトレードオフを理解して選ぶべき。

---

## 🔮 今後の展望

| フェーズ | 内容 | 状態 |
|---------|------|------|
| Phase 1 | 画像圧縮（Canvas API） | ✅ 完了 |
| Phase A | 画像圧縮（Rust/WASM化） | ✅ 完了 |
| Phase B | 動画圧縮（ffmpeg.wasm → MediaRecorder） | ✅ 完了 |
| Phase C | 動画圧縮（WebCodecs + HEVC対応） | ✅ 完了 |
| Phase D | 音声トラック処理（AAC無劣化コピー） | ✅ 完了 |
| Phase E | PWA化（オフライン動作） | 💡 検討中 |

---

*Developed by MorutonLaboratory with 🦀 Rust + WebAssembly + 🎬 WebCodecs*
